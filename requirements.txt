# Core
pydantic>=2.7
python-dotenv>=1.0
PyYAML>=6.0
structlog>=24.1

# Fetching
httpx>=0.27
feedparser>=6.0
beautifulsoup4>=4.12
trafilatura>=1.9

# Research input
pypdf>=6.0

# Raster images. The paper prints borrowed infographics (BKM section 14), which
# arrive as large PNGs and have to be resized and converted to webp before they
# go in the repository.
Pillow>=10.3

# Processing
rapidfuzz>=3.9

# Rendering
Jinja2>=3.1
markdown-it-py>=3.0

# Timezones. The paper's day is Asia/Jerusalem, and Windows ships no tz
# database, so zoneinfo needs this to resolve the zone off-Linux.
tzdata>=2024.1

# LLM (adjust once provider is chosen)
openai>=1.40

# Dev
pytest>=8.2
ruff>=0.5
